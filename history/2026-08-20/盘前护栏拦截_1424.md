# 盘前护栏拦截 (2026-08-20 1424)

**未生成盘前选股简报。**

原因：当前 14:24 已越过盘前截止 09:30。

盘前任务应在开盘前(默认截止 09:30)用前收盘价生成；此时已超过时点，数据源将返回盘中实时价，若仍以'盘前'名义给出买点，会在下跌途中诱导买入。

## 处理建议
1. 检查机器 8:30 是否因休眠/关机未触发本任务（排程本身正确）。
2. 若确需盘中参考，请改用盘中/盘后流程：
   - `python -m src.daily_brief --session post_market`（盘后）
   - `python -m src.afternoon_review`（15:30 复盘）
3. 本文件仅为拦截记录，不含任何买卖建议。
