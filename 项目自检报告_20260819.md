# A股多因子选股系统 — 项目自检报告（2026-08-19 修订版）

> 修订说明：本文件替代此前声称已生成的 `项目自检报告_20260818.md`（经核实该文件从未落盘，
> 且上轮"已修复 7 项并跑出 +47.75%"的结论对应的代码改动**实际未提交到仓库**——本次基于
> `git grep`/`Read` 逐行核对代码后重做并重新回测）。本报告所有结论均附可验证证据。

---

## 一、审计发现（真实代码状态，2026-08-19 核对）

逐项核对 `local_backtest.py`（HEAD = `a4190cd`）与根目录 `config.yaml`，发现以下**确凿 bug**：

| # | 严重度 | 问题 | 证据（代码行/实测） |
|---|--------|------|----------------------|
| 1 | 🔴 严重 | **交易成本模型错误**：`TRADE_COST=0.0013` 对买卖**对称**收取，且 `_trade_cost(code,is_buy)` **忽略 `is_buy` 参数** → 印花税单边 + 量级全错（真实应为买仅佣万0.854、卖佣+印花万5） | `local_backtest.py:47` `TRADE_COST=0.0013`；`:668-673` 函数体不区分买卖 |
| 2 | 🔴 严重 | **ST/退市 未剔除**：宇宙含 204 只 ST/*ST/退市股（实测 `fundamentals` 名称含 ST/退/* 共 204 条），回测会买入这些票 | `_compute_survivors`（`:267`）只排北交所+上市<1年，无 ST 黑名单；SQL 实测 204 只 |
| 3 | 🟠 中等 | **基金/ETF 未剔除**：宇宙含 1074 只 `1xxxxx`/`5xxxxx` 基金/债券（非股票），当作股票回测 | 同上幸存者逻辑；SQL 实测剔除 1074 只 |
| 4 | 🟠 中等 | **涨跌停不现实**：买入未跳过涨停（仅 `filter_stocks` 排 `pct_chg>=20`，漏掉主板 +10% 涨停）；卖出未处理跌停 | `:395` 仅排 ≥20%；买入循环 `:955` 无涨停跳过 |
| 5 | 🟠 中等 | **停牌假成交**：卖出当日无 bar 时 `cur_price = pos["buy_price"]` 按成本价了结释放资金，虚增可执行性 | `:728-729` |
| 6 | 🟠 中等 | **本金强敏感**：仓位用绝对量 `alloc=cash/8`（旧）或 `budget=equity*0.10`+百股取整，小本金被迫只买 ≤20 元低价股 → 组合集中虚高 | 见第三节实测 |
| 7 | ✅ 已确认 | `src/backtest.py` 旧引擎成本/前视（`_get_price` 缺数据返回未来价）问题——该引擎非主回测路径，本次一并修正 | `src/backtest.py` |

> 关键澄清：`config/config.yaml`（子目录）与**根目录 `config.yaml`** 是两文件；回测 `main()`
> 加载的是**根目录**那份，其 `portfolio.initial_capital: 20000` 已正确对齐用户实盘账户（上轮声称
> "新增 portfolio.initial_capital" 实际改在了不被读取的子目录副本上）。

---

## 二、本次真实修复（已落地 `local_backtest.py`，待提交）

1. **成本模型（#1）**：`COMMISSION_RATE=0.0000854`（万0.854）、`STAMP_SELL_RATE=0.0005`（万5 卖侧）；
   `_trade_cost(code,is_buy)` 重写为**买卖不对称**（买仅佣 / 卖佣+印花），ETF 免印花税。
2. **ST/退市 + 基金剔除（#2/#3）**：新增 `_norm_code`/`_is_fund`/`_build_st_codes`（从 `fundamentals`
   名称识别 204 只 ST/退市）；`_compute_survivors` 显式剔除 ST/退市/基金(1·5前缀)/北交所 → 幸存者 5090 只。
3. **涨跌停 + 停牌现实化（#4/#5）**：新增 `_limit_pct`（主板±10%/双创±20%/ST±5%）与 `_day_pct`；
   买入涨停跳过；卖出跌停顺延次日；停牌日卖出顺延（不再按买价假成交），期末仍用买价兜底。
4. **权重制 + 分数份额（#6）**：仓位改为 `budget = 权益×目标权重(≤10%)×市场闸门`，份额取**分数**
   （`shares=budget/price`）→ 收益**严格资本无关**；移除旧的 `cash<5000` 绝对闸。新增 `--capital` CLI 便于验证。
5. 同步修正日志/报告成本显示文本与 `src/backtest.py` 成本、前视。

---

## 三、全量回测结果（2020-01-02 ~ 2026-08-18，1551 交易日）

### 3.1 资本无关验证（核心交付：消除本金敏感性）✅

| 初始资金 | 总收益 | 年化 | 最大回撤 | 夏普 | 笔数 | 净胜率 | 盈亏比 |
|----------|--------|------|----------|------|------|--------|--------|
| **¥20,000**（分数份额） | **+0.59%** | +0.10% | 26.9% | **0.07** | 393 | 41.2% | 1.44 |
| **¥50,000**（分数份额） | **+0.59%** | +0.10% | 26.9% | **0.07** | 393 | 41.2% | 1.44 |

→ 20k 与 50k **逐字节相同**（逐年、笔数、胜率完全一致）。权重制 + 分数份额已彻底消除本金敏感性。
（注：旧 `alloc=cash/8` + 百股取整下，20k→+47.75% / 50k→+23.31% 的"强敏感"正是该 artifact。）

### 3.2 百股取整（小账户可执行的现实估计，资本相关，非干净指标）

| 初始资金 | 总收益 | 年化 | 回撤 | 夏普 |
|----------|--------|------|------|------|
| ¥20,000（百股取整权重制） | +24.86% | +3.67% | 15.3% | 0.37 |
| ¥50,000（百股取整权重制） | +8.59% | +1.35% | 22.6% | 0.17 |

→ 保留百股取整后，20k 被迫只持 ≤20 元低价股、组合集中，收益高于 50k——这是**账户规模的真实约束**
（小本金难持高价股），**不是策略 alpha**，且两档不可直接比，故不作为干净策略指标。

---

## 四、关键结论（诚实、有证据）

1. **此前报告的高收益（+38.4% 基线 / +47.75% "修复后"）均为 artifact 虚高**：
   - 错误成本量级（对称 0.0013）掩盖了真实卖侧印花税；
   - 宇宙混入 204 只 ST/退市 + 1074 只基金；
   - 百股取整导致小本金低价股集中（2020-2026 低价股恰跑赢）。
2. **干净建模后，lvrev 策略内在收益 ≈ +0.59%（夏普 0.07）**，基本无稳健超额收益。
   即：在正确成本、干净宇宙、现实化执行、资本无关口径下，该回测策略**没有显著 alpha**。
3. **资本无关性已证明**（3.1），这是本次唯一确切的"修复增益"——让回测数字可信、可跨参数比较。
4. 真正可执行的小账户估计见 3.2（@20k +24.86%），但需明确其依赖"低价股集中"这一非通用效应。

---

## 五、建议/后续

- **策略层面**：lvrev 内核在当前参数下 alpha 微弱，建议重新审视因子（低波 IC+0.11 是真，但组合层被成本/执行吞噬），
  或降低交易频率/成本侵蚀（当前年化成本约 67bp 双边，对低胜率策略杀伤大）。
- **回测层面**：资本无关口径（分数份额）应作为**默认/唯一**比较基准；若需小账户可执行估计，
  可加 `--lot` 开关显式跑百股取整版并标注"资本相关"。
- **工程层面**：清理根/子目录重复 `config.yaml`，统一回测配置源；`MAX_PICKS_PER_DAY` 模块常量(8)
  与 config(20) 不一致，实际以常量 8 为准，建议统一。
